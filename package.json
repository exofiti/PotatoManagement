{
  "name": "donation-bot",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "check": "node --check index.js && node --check deploy-commands.js && node --check config.js && node --check db.js && node --check lib/validation.js && node --check test/smoke.js && node --check test/validation.test.js",
    "deploy": "node deploy-commands.js",
    "smoke": "node test/smoke.js",
    "start": "node index.js",
    "test": "npm run check && npm run smoke && node --test test/validation.test.js"
  },
  "allowScripts": {
    "better-sqlite3": true
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "discord.js": "^14.15.3"
  }
}
